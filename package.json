{
  "name": "ttqs-one",
  "version": "0.6.0",
  "private": true,
  "description": "TTQS ONE TEST runtime candidate for 2026-08-13 consultant demo",
  "type": "module",
  "scripts": {
    "lint": "node scripts/lint-gs.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify:deploy": "node scripts/check-deploy-set.mjs",
    "verify:clasp-status": "node scripts/verify-clasp-status.mjs clasp-status.json",
    "verify": "npm run lint && npm test && npm run verify:deploy"
  },
  "engines": {
    "node": ">=22"
  }
}
