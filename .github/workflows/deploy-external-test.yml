name: Deploy External TEST Evaluator Portal

on:
  push:
    branches:
      - deploy/test
  workflow_dispatch:

concurrency:
  group: ttqs-one-deploy-external-test
  cancel-in-progress: false

permissions:
  contents: read
  issues: write

env:
  EXTERNAL_RECEIPT_ISSUE: '39'
  EXTERNAL_SCRIPT_TITLE: 'TTQS ONE TEST External Evaluator Portal'

jobs:
  deploy_external:
    if: github.ref == 'refs/heads/deploy/test'
    runs-on: ubuntu-24.04
    environment: TEST
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          fetch-depth: 0

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: '22'
          cache: npm

      - name: Verify deploy ref and main ancestry
        shell: bash
        run: |
          set -euo pipefail
          test "$GITHUB_REF" = "refs/heads/deploy/test"
          git fetch origin main --no-tags
          git merge-base --is-ancestor "$GITHUB_SHA" origin/main
          printf 'External TEST deploy source SHA: %s\n' "$GITHUB_SHA"

      - name: Mark external deployment receipt RUNNING
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          gh issue view "$EXTERNAL_RECEIPT_ISSUE" --repo "$GITHUB_REPOSITORY" --json body --jq .body > external-receipt-body.md
          node scripts/external-deployment-receipt.mjs --input external-receipt-body.md --output external-receipt-next.md --state RUNNING
          gh issue edit "$EXTERNAL_RECEIPT_ISSUE" --repo "$GITHUB_REPOSITORY" --body-file external-receipt-next.md

      - name: Verify repository and external viewer safety contract
        shell: bash
        run: |
          set -euo pipefail
          npm ci --ignore-scripts
          npm run verify
          node -e "const fs=require('fs'); const manifest=JSON.parse(fs.readFileSync('external-viewer/appsscript.json','utf8')); const expectedScope='https://www.googleapis.com/auth/spreadsheets.readonly'; const expectedService=[{userSymbol:'Sheets',version:'v4',serviceId:'sheets'}]; if(manifest.webapp?.access!=='ANYONE_ANONYMOUS') throw new Error('EXTERNAL_ACCESS_NOT_ANONYMOUS'); if(manifest.webapp?.executeAs!=='USER_DEPLOYING') throw new Error('EXTERNAL_EXECUTION_IDENTITY_INVALID'); if(JSON.stringify(manifest.oauthScopes)!==JSON.stringify([expectedScope])) throw new Error('EXTERNAL_SCOPE_INVALID'); if(JSON.stringify(manifest.dependencies?.enabledAdvancedServices)!==JSON.stringify(expectedService)) throw new Error('EXTERNAL_SHEETS_SERVICE_INVALID'); const files=fs.readdirSync('external-viewer').sort(); if(JSON.stringify(files)!==JSON.stringify(['Code.gs','appsscript.json'])) throw new Error('EXTERNAL_PUSH_SET_INVALID');"

      - name: Restore TEST OAuth credential material
        shell: bash
        env:
          CLASPRC_JSON_B64: ${{ secrets.CLASPRC_JSON_B64 }}
        run: |
          set -euo pipefail
          test -n "$CLASPRC_JSON_B64"
          OAUTH_FILE="$RUNNER_TEMP/ttqs-external-oauth.json"
          printf '%s' "$CLASPRC_JSON_B64" | base64 -d > "$OAUTH_FILE"
          chmod 600 "$OAUTH_FILE"
          echo "EXTERNAL_OAUTH_FILE=$OAUTH_FILE" >> "$GITHUB_ENV"

      - name: Verify minimal OAuth deployment contract
        shell: bash
        run: |
          set -euo pipefail
          node scripts/apps-script-rest-deploy.mjs auth-check --credentials "$EXTERNAL_OAUTH_FILE"

      - name: Read durable external deployment receipt
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          gh issue view "$EXTERNAL_RECEIPT_ISSUE" --repo "$GITHUB_REPOSITORY" --json body --jq .body > external-receipt-body.md
          node --input-type=module -e "import fs from 'node:fs'; import {parseReceiptBody} from './scripts/external-deployment-receipt.mjs'; const r=parseReceiptBody(fs.readFileSync('external-receipt-body.md','utf8')); const s=String(r.scriptId||''); const d=String(r.deploymentId||''); if(s&&!/^[A-Za-z0-9_-]{20,}$/.test(s)) throw new Error('EXTERNAL_RECEIPT_SCRIPT_ID_INVALID'); if(d&&!/^AKfy[A-Za-z0-9_-]+$/.test(d)) throw new Error('EXTERNAL_RECEIPT_DEPLOYMENT_ID_INVALID'); if(d&&!s) throw new Error('EXTERNAL_RECEIPT_DEPLOYMENT_WITHOUT_SCRIPT'); fs.appendFileSync(process.env.GITHUB_ENV,'EXTERNAL_SCRIPT_ID='+s+'\\nEXTERNAL_DEPLOYMENT_ID='+d+'\\n');"

      - name: Recover or bootstrap external TEST Apps Script project via REST
        shell: bash
        run: |
          set -euo pipefail
          node scripts/apps-script-rest-deploy.mjs ensure-project \
            --credentials "$EXTERNAL_OAUTH_FILE" \
            --script-id "$EXTERNAL_SCRIPT_ID" \
            --title "$EXTERNAL_SCRIPT_TITLE" \
            --env-file "$GITHUB_ENV"

      - name: Persist external script identity before content push
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          gh issue view "$EXTERNAL_RECEIPT_ISSUE" --repo "$GITHUB_REPOSITORY" --json body --jq .body > external-receipt-body.md
          node scripts/external-deployment-receipt.mjs --input external-receipt-body.md --output external-receipt-next.md --state RUNNING
          gh issue edit "$EXTERNAL_RECEIPT_ISSUE" --repo "$GITHUB_REPOSITORY" --body-file external-receipt-next.md

      - name: Push external TEST viewer via Apps Script REST
        shell: bash
        run: |
          set -euo pipefail
          node scripts/apps-script-rest-deploy.mjs push-content \
            --credentials "$EXTERNAL_OAUTH_FILE" \
            --script-id "$EXTERNAL_SCRIPT_ID_RESOLVED" \
            --root-dir external-viewer

      - name: Create or update external TEST web app deployment via REST
        shell: bash
        run: |
          set -euo pipefail
          DESCRIPTION="TTQS_ONE_TEST_EXTERNAL $GITHUB_SHA"
          node scripts/apps-script-rest-deploy.mjs deploy \
            --credentials "$EXTERNAL_OAUTH_FILE" \
            --script-id "$EXTERNAL_SCRIPT_ID_RESOLVED" \
            --deployment-id "$EXTERNAL_DEPLOYMENT_ID" \
            --description "$DESCRIPTION" \
            --env-file "$GITHUB_ENV"

      - name: Persist external deployment identity before black-box probe
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          gh issue view "$EXTERNAL_RECEIPT_ISSUE" --repo "$GITHUB_REPOSITORY" --json body --jq .body > external-receipt-body.md
          node scripts/external-deployment-receipt.mjs --input external-receipt-body.md --output external-receipt-next.md --state RUNNING
          gh issue edit "$EXTERNAL_RECEIPT_ISSUE" --repo "$GITHUB_REPOSITORY" --body-file external-receipt-next.md

      - name: Anonymous product black-box probe
        shell: bash
        run: |
          set -euo pipefail
          URL="$EXTERNAL_WEBAPP_URL"
          test -n "$URL"
          PASS=0
          for attempt in 1 2 3 4 5 6; do
            rm -f external-page.html
            if curl --fail --silent --show-error --location --max-time 30 "$URL" -o external-page.html; then
              if grep -Fq 'TTQS ONE' external-page.html \
                && grep -Fq 'SAMPLE 評核因果鏈' external-page.html \
                && grep -Fq '查看佐證與來源' external-page.html \
                && grep -Fq 'EXTERNAL_READONLY' external-page.html \
                && grep -Fq '19 / 19' external-page.html \
                && ! grep -Fq '目前無法載入唯讀快照' external-page.html; then
                PASS=1
                break
              fi
            fi
            sleep 10
          done
          if test "$PASS" -ne 1; then
            echo 'EXTERNAL_PRODUCT_BLACKBOX_FAIL'
            if test -f external-page.html; then sed -n '1,80p' external-page.html; fi
            exit 1
          fi
          echo 'EXTERNAL_PRODUCT_BLACKBOX_PASS'

      - name: Publish durable deployment receipt after black-box PASS
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          gh issue view "$EXTERNAL_RECEIPT_ISSUE" --repo "$GITHUB_REPOSITORY" --json body --jq .body > external-receipt-body.md
          node scripts/external-deployment-receipt.mjs --input external-receipt-body.md --output external-receipt-next.md --state PASS_PRODUCT_BLACKBOX
          gh issue edit "$EXTERNAL_RECEIPT_ISSUE" --repo "$GITHUB_REPOSITORY" --body-file external-receipt-next.md

      - name: Record deployment provenance
        shell: bash
        run: |
          set -euo pipefail
          {
            echo '## TTQS ONE External TEST deployment'
            echo
            echo "- Source SHA: $GITHUB_SHA"
            echo "- Script ID: $EXTERNAL_SCRIPT_ID_RESOLVED"
            echo "- Deployment ID: $EXTERNAL_DEPLOYMENT_ID_RESOLVED"
            echo "- Version: $EXTERNAL_VERSION_NUMBER"
            echo "- URL: $EXTERNAL_WEBAPP_URL"
            echo '- Provider path: Apps Script REST API with minimal user OAuth scopes'
            echo '- Access: ANYONE_ANONYMOUS'
            echo '- Execute as: USER_DEPLOYING'
            echo '- Data: TEST/SAMPLE deidentified snapshot only'
            echo '- Anonymous black-box: PASS'
            echo '- Receipt: GitHub Issue #39'
            echo '- REAL/PROD: not performed'
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Publish FAILED receipt for observability
        if: ${{ failure() }}
        continue-on-error: true
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          gh issue view "$EXTERNAL_RECEIPT_ISSUE" --repo "$GITHUB_REPOSITORY" --json body --jq .body > external-receipt-body.md
          node scripts/external-deployment-receipt.mjs --input external-receipt-body.md --output external-receipt-next.md --state FAILED
          gh issue edit "$EXTERNAL_RECEIPT_ISSUE" --repo "$GITHUB_REPOSITORY" --body-file external-receipt-next.md
